Zlata Space V3 — audio-fixed build

The track is a clean MP3 (audio stream only, no embedded MJPEG cover/metadata).
Playback uses the browser's native HTMLAudioElement so clicking Play is not delayed by Web Audio initialization.
The visualizer is decorative and does not sit in the audio signal path.

Open index.html from this folder or serve the folder with any static web server.
